# v0.1 full-surface audit — all 49 confirmed findings (with fixes)

> Source: adversarial audit workflow `wf_c0926183-0d4` (2026-07-12), 63 agents, 0 errors.
> 11 HIGH / 22 MEDIUM / 16 LOW confirmed after default-to-refute verification.
> **10 commits already fixed all 11 HIGH + restore + canary + store-trap** (see the remediation
> plan `v0.1-remediation-plan.md` for the commit map). This file carries the FULL fix detail for
> every finding so the remaining MEDIUM/LOW + 3 features can be executed with no context loss.

> DONE so far: setup.sh:26, cmd/list.sh:33 (jq), uninstall.sh:34, restore.sh:18, FAQ:14,
> SECURITY:41/README:263/store.sh:188 (canary), bin/agent-secrets:80 (smoke), setup.sh:101 (restore),
> doctor launchd-label, list set-e, run usage, exit-2 batch, store_add interrupt-trap.
> REMAINING: baked-SHA (install.sh:22 + .gitattributes), egress allowlist, GitHub backup, and the
> doc/consistency MEDIUM/LOW (doctor-json schema in help.sh/AGENTS, "exactly what it changes",
> multi-line inject decode, receive temp-file FAQ, settings.json rollback edge, ls/version help,
> share --to in AGENTS table, --sign/allowed_signers claim, local-docs-not-shipped, etc.).

## [HIGH] cmd/setup.sh:26  (core-verbs-a/correctness)
setup's key-ceremony idempotency guard requires BOTH age.key and age.pub to be non-empty, so a present private key with a missing/empty public key permanently wedges setup with a silent crash, contradicting the documented idempotency/resume contract.
FIX: In _key_ceremony (setup.sh:24-30): gate resume on the private key alone, derive the missing public recipient, and clear a truncated key so -o can recreate; also stop swallowing keygen errors. e.g.:
  local kf pf; kf="$(agsec_age_key_file)"; pf="$(agsec_age_pub_file)"
  if [ -s "$kf" ]; then
    [ -s "$pf" ] || age-keygen -y "$kf" >"$pf"     # regenerate the non-secret public key when absent
    ui_ok "key already exists — not minting a second one"; kc_write_selector; store_init; return 0
  fi
  [ -f "$kf" ] && rm -f "$kf"                        # drop a 0-byte/partial key from an interrupted mint
  age-keygen -o "$kf"; chmod 600 "$kf"               # drop 2>/dev/null so a genuine keygen failure is visible
Leave line 30 (age-keygen -y ... >age.pub) as-is for the fresh path.

## [HIGH] cmd/list.sh:33  (core-verbs-a/error-handling)
`list --format=json` invokes jq unguarded, so on a machine without jq it fails with a raw shell error (exit 127) instead of a clean diagnostic, unlike every sibling verb that guards jq.
FIX: Add jq to the installer's dependency bootstrap: `brew install age sops gum jq` at install.sh:76, and document jq as a required dependency (README/SECURITY/FAQ). Do NOT merely guard list.sh:33 — jq is hard-required unguarded across lib/manifest.sh (install/uninstall backbone), install.sh:113, and lib/help.sh:187, so provisioning it at install time is the root fix; a per-verb guard would leave the installer, uninstall/rollback, and help --format=json still broken on a jq-less host.

## [HIGH] cmd/uninstall.sh:34  (core-verbs-b/residue-after-uninstall)
Under `set -euo pipefail`, the bare `read -r ans` returns non-zero at stdin EOF and aborts the script BEFORE manifest_rollback runs, so a non-interactive uninstall exits 1 having removed nothing and leaving total tool residue.
FIX: Fail-closed to KEEP-and-proceed on EOF so the tool-artifact rollback always runs. Replace line 34 `local ans; read -r ans` with `local ans=''; read -r ans || ans=''` (the `|| ans=''` puts read in an OR-list, exempting it from set -e). Case then matches `*` → purge=0 (KEEP), and manifest_rollback (data-safe artifact removal) executes; only the destructive store purge stays gated behind an affirmative `y`. Equivalent: guard with `if [ -t 0 ]; then read -r ans; else ans=''; fi`. Optionally emit an agsec_note when stdin is non-interactive so the failure-to-purge default is visible.

## [HIGH] lib/restore.sh:18  (lib-foundation/broken-documented-contract)
restore_flow() is unreachable — no verb or setup branch calls it — so the documented full-machine 'restore option' in the setup wizard does not exist.
FIX: Wire restore_flow into the shipping path and align docs. Minimal: in cmd/setup.sh main(), before _key_ceremony, add an interactive branch matching the FAQ wording — e.g. when not UNATTENDED, `if ui_confirm "Restoring on a new machine? (paste your saved key)" n; then restore_flow && { _wire_tools; _state restored; return 0; }; fi` — so "choose the restore option" actually exists (or add a dedicated `restore` dispatcher verb that calls restore_flow). Then adjust FAQ.md:14-15 and SECURITY.md:77 to name whatever surface actually ships.

## [HIGH] docs/FAQ.md:14  (docs-readme-faq/recovery-contract-broken)
The documented disaster-recovery procedure ('run agent-secrets setup, choose the restore option, and paste your saved key — your store decrypts') has no corresponding CLI surface; setup offers no restore option and instead mints a brand-new key.
FIX: Pick one and make docs match code: (A) Wire the existing restore_flow() to a real surface — add a `restore` verb (cmd/restore.sh → restore_flow) and/or a "Restore an existing store from your saved key?" branch in setup's returning-user case (on 'fresh'/'partial', offer restore_flow before _key_ceremony mints a new key), then update FAQ.md:14-16 and SECURITY.md:77-78 to name that surface; or (B) if no restore UI ships in v0.1, rewrite FAQ.md:14-16 and SECURITY.md:77-78 to the actual manual procedure (restore ~/.config/secrets/ including age.key first, then run setup, which detects it as 'partial' and reuses the key) and delete the phrases "choose the restore option" and "the setup wizard's restore branch." Do not document a setup restore option that does not exist.

## [HIGH] README.md:263  (docs-readme-faq/security-claim-unbacked)
The honest-ceiling claim that 'an in-store canary trips an alert on any whole-store sweep' is inert as shipped — the seeded canary value is a static placeholder that is never replaced with a real canarytokens.org token.
FIX: Either have onboarding mint and insert a real canarytokens.org token (and document the step), or reword README:263/SECURITY.md to state the canary is a decoy placeholder that only alerts once the user wires a real canarytoken.

## [HIGH] SECURITY.md:41  (docs-agents-security-llms/false-security-claim)
SECURITY.md presents the in-store canary as a functional canarytokens.org honeytoken active by default, but the shipped store seeds an inert placeholder string that no onboarding step or documented action ever replaces — so the sole free-tier detection control the 'honest ceiling' rests on does nothing as installed.
FIX: Either wire a real canarytokens.org token into onboarding (add a setup step that mints/pastes a live token and a doctor check that flags the still-placeholder value), or correct SECURITY.md/README.md to state the shipped canary is an inert placeholder the operator must replace to get detection, add the replace instructions, and reconcile FAQ.md's 'enable' wording with a real enable path.

## [HIGH] bin/agent-secrets:80  (shell-robustness-all/correctness)
The dispatcher has no `smoke` case, but the installed launchd job runs `agent-secrets smoke`, so the weekly maintenance job fails with exit 2 every week and never runs.
FIX: In bin/agent-secrets, add `smoke) _dispatch smoke "$@" ;;` to the main case table (e.g., immediately after the `pubkey) _dispatch pubkey "$@" ;;` arm at line 80). AGENT_SECRETS_LIB/CMD are already exported (line 18) and _dispatch execs `bash cmd/smoke.sh`, so no other wiring is needed. Keep `smoke` OUT of the per-command help-interception case (line 64) and out of help/--json so it stays a hidden maintenance verb. Also correct the now-misleading comment at cmd/smoke.sh:2 ("Run by the launchd job (NOT a dispatcher verb)") — it IS routed through the dispatcher.

## [HIGH] cmd/setup.sh:101  (security-invariant-e2e/broken-documented-contract)
The documented 'paste your saved key' restore flow is never wired into the CLI; a fresh-machine setup mints a NEW key that cannot decrypt a backed-up store.
FIX: Wire restore_flow into the wizard: when restore_returning_user_check reports 'fresh' but a store file is present (or via an explicit 'agent-secrets setup --restore' / menu option), prompt to paste the saved age key and call restore_flow() BEFORE _key_ceremony mints a new key; guard _key_ceremony from minting when an undecryptable store already exists.

## [HIGH] lib/store.sh:188  (security-invariant-e2e/security-claim-not-implemented)
The in-store canary is seeded as a static placeholder that trips no alert, yet README/SECURITY.md present it as an active honeytoken and doctor reports it 'readable' (false assurance).
FIX: Pick ONE and make the docs+tool consistent. (A) Honest-inert framing (smallest change, preserves $0/local design): reword SECURITY.md:41-44 and README.md:263 to say the store ships an INERT decoy slot the user must arm by pasting a self-minted canarytokens.org token into the AWS_BACKUP_ACCESS_KEY_ID entry, add that documented step (e.g. `agent-secrets add AWS_BACKUP_ACCESS_KEY_ID` fed a real token) to the onboarding docs, and fix FAQ.md:42 to reference that concrete step. Then have doctor.sh:96-100 compare store_extract "$AGENT_SECRETS_CANARY_NAME" against the placeholder string and, on match, emit an `attn` row 'canary — placeholder, NOT armed (paste a canarytokens.org token to enable detection)' instead of the green 'canary readable'; keep a separate decrypt self-test row so the sops-health check isn't conflated with tripwire status. (B) If the unconditional 'trips an alert' claim is meant to hold out-of-the-box, add a real minting/arming step to setup.sh and mint per-user — but that adds a network dependency and is larger. (A) is the minimal fix.

## [HIGH] install.sh:22  (security-invariant-e2e/supply-chain-integrity)
The installer's SHA-256 'trust anchor' is empty, so it verifies the tarball against a .sha256 file fetched from the same origin — no integrity guarantee against a compromised release, contradicting its own security claim.
FIX: At release-cut, compute the tarball digest and bake it into install.sh's EXPECTED_SHA256 before tagging/publishing, and add that step explicitly to the release procedure (README:300, which currently only writes the sibling .sha256). Keep the curl'd `$url.sha256` fallback gated to the dev/mirror path (e.g. only when AGENT_SECRETS_BASE_URL is overridden), not the default production install. Until the bake-in ships, soften README:81/install.sh:93 wording from "pins a SHA-256–verified release"/"SHA-256 verified" to reflect that the default path verifies against a co-located digest (transport integrity), so the advertised guarantee matches deployed behavior.

## [MEDIUM] cmd/doctor.sh:26  (core-verbs-b/broken-contract)
doctor's maintenance check queries launchd label 'com.agent-secrets.weekly-smoke' but install.sh registers the job as 'com.agent-secrets.smoke', so the check is guaranteed-wrong on every correct install.
FIX: Change doctor.sh:26 default to `com.agent-secrets.smoke` to match install.sh's SMOKE_LABEL, or better, define the label as a single AGENT_SECRETS_LAUNCHD_LABEL constant in lib/common.sh and have BOTH install.sh and doctor.sh consume it so write and check can never drift.

## [MEDIUM] cmd/smoke.sh:70  (core-verbs-b/false-alarm)
The weekly smoke job sets fail=1 (exit 1 + user notification) whenever npm is absent or ignore-scripts is unset, treating an advisory as a hard failure and inconsistent with doctor which treats the same check as non-failing `attn`.
FIX: Mirror doctor's advisory semantics in smoke check (6). Replace smoke.sh:69-70 so an absent npm or unset ignore-scripts is advisory, not a smoke failure: e.g. `if agsec_have npm && _try v npm config get ignore-scripts && [ "$v" = true ]; then agsec_ok "ignore-scripts"; else agsec_attn "ignore-scripts (advisory: run: npm config set ignore-scripts true)"; fi` — drop the `notify ... ; fail=1`. Reserve fail=1 for checks 1-4 (the genuine health checks). This removes the doctor/smoke divergence and the internal agsec_attn+fail=1 contradiction.

## [MEDIUM] install.sh:22  (install-wrappers/security-claim-false)
The deployed v0.1.0 installer ships EXPECTED_SHA256="" with no release-time baking step, so SHA-256 verification always falls back to a `.sha256` fetched from the SAME release surface as the tarball — same-origin verification that cannot defend against the release-asset tampering it purports to catch.
FIX: Add a release step that bakes the real tarball digest into install.sh before the git tag (so the pin travels the git-ref channel, distinct from the release-asset tarball). Because install.sh currently ships inside the tarball (it is NOT in .gitattributes export-ignore), baking is circular — first export-ignore install.sh from the tarball (it is delivered via raw-git anyway) or compute the digest over the archived tree excluding install.sh, then write that value into EXPECTED_SHA256 and tag. Until baking exists, correct the install.sh:19-21 comment to stop asserting "baked in at release-tag time / the digest lives here" and to state plainly that the shipped release verifies against the same-origin sibling .sha256 (corruption/partial-download protection, not release-asset tamper-evidence). Leave README:81 as-is.

## [MEDIUM] install.sh:135  (install-wrappers/rollback-residue)
The settings.json revert-point is recorded only if ~/.claude/settings.json already exists; when it doesn't, rollback restores an empty `{}` (residue), and because setup re-copies its backup on every re-run, a second `setup` makes rollback restore a settings.json still pointing at the now-deleted ~/bin/apiKeyHelper.
FIX: Two minimal changes: (1) setup.sh:78 — make the backup write-once so a re-run cannot overwrite the pristine/absent-derived state: replace `cp "$sj" "$bak"` with `[ -f "$bak" ] || cp "$sj" "$bak"`. (2) Preserve the never-existed case: capture absence BEFORE line 77 creates `{}` (e.g. `local existed=1; [ -f "$sj" ] || existed=0`), and when `existed=0` record the edit with a sentinel/absent marker so rollback deletes ~/.claude/settings.json instead of recreating `{}` (extend manifest_record_edit + _manifest_rb_edit to honor an "absent" backup by `rm -f` on the path). Alternatively (cleaner): drop setup's duplicate edit record entirely and let install.sh own the settings.json revert — recording it write-once even when the file is absent, with an absent marker that rollback rm's — so there is a single, correct revert point regardless of re-runs.

## [MEDIUM] lib/restore.sh:22  (lib-foundation/correctness)
restore_flow's interactive (TTY) branch reads the age key with single-line ui_read_secret, but a saved age key is a 3-line file, so it captures only the leading comment.
FIX: Route multi-line age-key material identically in both branches of restore_flow: drop ui_read_secret for the key and, in the TTY branch, prompt "paste your saved key, then press Ctrl-D" and use `key="$(cat)"` (the same multi-line-safe read the non-TTY branch and kc_add already use), or grep out just the `AGE-SECRET-KEY-1` line before storing. Separately, reconcile the docs with reality: either actually wire restore_flow into setup as a selectable "restore" path (then this fix makes it work), or correct FAQ.md:14-15 and SECURITY.md:77-78 to describe the restore path that truly exists. Add a TTY-path (or extract-AGE-SECRET-KEY-line) test so the interactive branch stops being unexercised.

## [MEDIUM] lib/store.sh:60  (lib-foundation/edge-case-data-integrity)
store_add reads the value with a single `IFS= read -r`, so a multi-line value piped to `agent-secrets add` is silently truncated to its first line with no error.
FIX: In store_add (store.sh:60), after the first `IFS= read -r value`, detect additional STDIN and fail closed instead of reporting success: `if IFS= read -r _rest || [ -n "$_rest" ]; then agsec_die "add takes a single-line value; multi-line secrets are not supported"; fi` (the extra line lands in a shell variable exactly as `value` already does — no new names-only exposure), or route multi-line input through store_add_multiline. Never silently drop trailing lines while printing "stored".

## [MEDIUM] README.md:263  (docs-readme-faq/security-claim-overstated)
The honest-ceiling says 'What this design adds is ... a process-scoped egress allowlist [that] bounds where a compromised agent can send data', but the tool never installs or configures any egress allowlist.
FIX: Reword README.md:262-264 so the egress bound is attributed to a third-party firewall the user installs/configures (which doctor gate (e) merely detects), not to the tool. E.g.: "... an in-store canary trips an alert on any whole-store sweep, and it detects (via doctor --gates) whether a process-scoped egress firewall such as LuLu or Little Snitch — which you install and configure — is present to bound where a compromised agent can send data." Align with FAQ.md:59's "the network bound this design asks for."

## [MEDIUM] docs/FAQ.md:20  (docs-readme-faq/doc-feature-nonexistent)
The backup FAQ says 'If you let the tool create a private GitHub repo for your store, it's already backed up there' — no such repo-creation feature exists anywhere in the tool.
FIX: Delete the first sentence of FAQ.md:20 entirely; the backup answer should describe only the real path — keep a second copy of ~/.config/secrets/. Also remove the matching phantom reference in setup.sh:21 (change the `gh` line to a plain optional-tool note, or drop the "only for the private store repo" clause) so the nonexistent feature isn't threaded through two files. Alternatively, actually implement a store-repo backup flow — but as shipped, remove the claim.

## [MEDIUM] docs/FAQ.md:22  (docs-readme-faq/doc-false-claim)
'agent-secrets doctor warns you when it has no second copy' is false — doctor has no backup/second-copy check.
FIX: Delete the false promise from both surfaces (a real check isn't feasible for an off-machine copy). FAQ.md:21-22 → end the sentence at 'keep a second copy of ~/.config/secrets/ somewhere safe.' and drop 'agent-secrets doctor warns you when it has no second copy.' setup.sh:92 → change to 'Local-only store? keep a second copy somewhere safe.' (remove the '(doctor warns if you don't)' parenthetical). Optionally reword to point at the GitHub-repo backup path that doctor could actually detect, but the minimal fix is removing the unbacked claim.

## [MEDIUM] README.md:87  (docs-readme-faq/doc-incomplete)
'Exactly what it changes on your Mac' is presented as exhaustive but omits several real changes: the gum package, the three ~/bin wrappers, the weekly launchd smoke job, and the ~/.claude/settings.json apiKeyHelper edit.
FIX: Expand the README:87-89 list to match the reversible diagram and uninstall description: add gum to the Homebrew line, name the claude-agent/cursor-agent/apiKeyHelper wrappers alongside the agent-secrets command, add the weekly launchd smoke job, and note the ~/.claude/settings.json apiKeyHelper edit (applied at setup). Alternatively soften "Exactly what it changes" so it no longer claims exhaustiveness.

## [MEDIUM] AGENTS.md:84  (docs-agents-security-llms/broken-recipe)
The 'Machine-readable health for decisions' recipe assumes doctor --format=json is a top-level array whose statuses are 'fail'; the real payload is an object with a checks[] array and statuses ok/attn/bad, so the recipe errors out.
FIX: Replace AGENTS.md:84 with `agent-secrets doctor --format=json | jq '[.checks[] | select(.status=="bad")]'` (object has a checks[] array; failing checks are status "bad", matching doctor's exit=1). Optionally add a one-line note of the JSON schema next to Golden rule 4 since it is documented nowhere else.

## [MEDIUM] lib/help.sh:75  (docs-agents-security-llms/manifest-inaccuracy)
The authoritative `help --json` manifest — which AGENTS.md:42-45 tells agents is where 'Everything you need to construct a valid invocation is' — describes doctor --format=json as 'array of {category, status, detail}', but the real output is an object {checks:[{category,status,check,detail}],exit:N}.
FIX: In lib/help.sh:75, change the flag description to: 'machine-readable results — object {checks:[{category,status,check,detail}],exit:N}; exit=1 iff any status is bad; no values'. (status ∈ ok|attn|bad.) Adjacent, out of scope of the finding line but should be fixed together: the AGENTS.md:84 recipe `jq '[.[] | select(.status=="fail")]'` is broken against the real shape — correct to `jq '[.checks[] | select(.status=="bad")]'`, since `.[]` on the object yields no `.status` and the token is `bad` not `fail`, silently returning `[]` (false-healthy).

## [MEDIUM] lib/help.sh:75  (help-selfdescribe/self-description-accuracy)
doctor's `--format=json` documented schema is wrong: it is not a bare 'array of {category, status, detail}' — it is an object `{"checks":[...],"exit":N}` whose rows carry an undocumented `check` field.
FIX: Rewrite lib/help.sh:75 to match the real schema, e.g.: `doctor	flag	--format=json	machine-readable object {"checks":[{category,status,check,detail}],"exit":0|1}; no values`. (Parse with `jq '.checks[] | select(.status=="bad")'`, not `.[]`.)

## [MEDIUM] lib/help.sh:39  (help-selfdescribe/reads-writes-mismatch)
The `setup` verb's `writes` field claims setup installs a 'launchd smoke job', but cmd/setup.sh never creates any launchd plist — install.sh does.
FIX: In lib/help.sh:39 rewrite the setup writes field to drop the launchd job (which belongs to install.sh) and include the config-dir files setup actually creates, e.g.: `~/.config/secrets/{age.key,age.pub,recovery.pub,age-key-cmd.sh,secrets.env,.sops.yaml,manifest.toml}, ~/bin wrappers, ~/.claude/settings.json (apiKeyHelper)`. Leave recovery.key out (it is shredded during the ceremony).

## [MEDIUM] cmd/doctor.sh:26  (help-selfdescribe/broken-health-check)
doctor's maintenance check looks for launchd label `com.agent-secrets.weekly-smoke`, but install.sh installs the job under `com.agent-secrets.smoke`, so the 'weekly smoke job loaded' check can never pass on a correctly installed system.
FIX: Change doctor.sh:26 default to the canonical install label: `LAUNCHD_LABEL="${AGENT_SECRETS_LAUNCHD_LABEL:-com.agent-secrets.smoke}"`. install.sh already uses `com.agent-secrets.smoke` in five self-consistent places (plist filename+Label, bootstrap, manifest), so aligning doctor to it is the single-line minimal fix.

## [MEDIUM] cmd/doctor.sh:26  (shell-robustness-all/correctness)
doctor's maintenance check defaults to launchd label `com.agent-secrets.weekly-smoke`, but install.sh registers the job under `com.agent-secrets.smoke`, so doctor always reports the smoke job as not loaded even on a correct install.
FIX: Change the doctor.sh:26 default from `com.agent-secrets.weekly-smoke` to `com.agent-secrets.smoke` so it matches install.sh:30; better, hoist the label into a single shared constant (e.g. in lib/common.sh) that both install.sh and doctor.sh source, and add a test asserting doctor's default label equals the label install.sh bootstraps.

## [MEDIUM] cmd/list.sh:42  (shell-robustness-all/shell-correctness)
`r="$(_rotate_of "$n")"` propagates the helper pipeline's non-zero exit under `set -e`, so `list` (text mode) truncates to just the header and exits non-zero when manifest.toml is missing or any store name lacks a manifest row.
FIX: Neutralize the failure at the call site: `r="$(_rotate_of "$n" || true)"` (both the text loop and, for symmetry, the JSON `--arg`), or make `_rotate_of` end in `|| true`. Also guard `[ -f "$man" ]` before the loop.

## [MEDIUM] lib/store.sh:62  (shell-robustness-all/shell-correctness)
store_add / store_add_multiline stage the entire decrypted store as a plaintext mktemp temp with no EXIT/signal trap, so an interrupt leaves all secret values in cleartext on disk, contradicting the 'staged ... and shredded' guarantee.
FIX: In store_add and store_add_multiline, register cleanup of the plaintext temps right after the mktemp, mirroring receive.sh:100 — but SCOPE it so it does not clobber the caller's EXIT trap (receive.sh calls these as in-process functions and already holds an EXIT trap). Wrap the decrypt→stage→sops-e→mv block in a subshell with a local trap, e.g.: `( trap 'rm -f "$plain" "$plain.f" "$store.new" 2>/dev/null' EXIT; <stage+encrypt+swap> ) || agsec_die "store_add: sops encrypt failed"`, or save and restore the prior EXIT trap around the region. Keep rm -f to match the existing _store_shred/FileVault-erasure semantics. Defense-in-depth: have doctor sweep stale $(agsec_config_dir)/.agsec.* temps.

## [MEDIUM] lib/store.sh:81  (security-invariant-e2e/correctness)
Multi-line secrets stored via receive are base64-encoded but no injection consumer decodes them, so run/wrappers/apiKeyHelper inject base64 text instead of the real value.
FIX: Two honest options. Minimal/immediate: at cmd/receive.sh:161-162, before store_add_multiline, agsec_warn that a multi-line value is stored but is NOT correctly run-injectable (child env receives base64), and add this limitation to user-facing docs + the receive help text; better, hard-refuse multi-line receive until injection is fixed. Proper fix: mark multi-line entries (manifest field or value sentinel) and decode on inject — but note store_exec relies on `sops exec-env` which cannot per-value decode, so this requires replacing exec-env with a manual decrypt→per-entry base64-decode→export→exec path (and store_extract must base64 -D flagged entries). Also fix the store.sh:77-78 comment, which currently claims a decode contract the shipped code never fulfills.

## [MEDIUM] AGENTS.md:84  (security-invariant-e2e/misleading-doc)
The documented agent recipe for parsing doctor --format=json errors out, and help.sh mis-describes the JSON schema, so an agent following the authoritative manifest cannot parse doctor output.
FIX: Fix AGENTS.md:84 to `agent-secrets doctor --format=json | jq '[.checks[] | select(.status=="bad")]'` (iterate .checks, and match the real failing status "bad"). Correct lib/help.sh:75 to describe the actual shape: object `{checks:[{category,status,check,detail}], exit}`; no values.

## [MEDIUM] README.md:263  (security-invariant-e2e/misleading-security-claim)
README claims the design adds 'a process-scoped egress allowlist' that bounds exfiltration, but the tool ships no egress control — it only detects whether a third-party firewall app is installed.
FIX: Reword README.md:263-264 so egress is framed as an external, user/corp-provided control the design RECOMMENDS and DETECTS (matching SECURITY.md/FAQ.md), not one it ships. E.g.: "…an in-store canary trips an alert on any whole-store sweep, and the design pairs that with a user-provided, process-scoped egress firewall (LuLu / Little Snitch / a corporate proxy) — which doctor --gates detects — to bound where a compromised agent can send data." Drop "What this design adds is … an egress allowlist" so the tool no longer implies it provides the bound.

## [MEDIUM] install.sh:22  (release-integrity/release-integrity-security-claim-false)
The deployed installer ships EXPECTED_SHA256="", so its own "pinned digest as trust root" design (install.sh:19-21) and README:81's claim that the installer "pins a SHA-256-verified release" are NOT fulfilled — tarball integrity is verified only against a sibling .sha256 co-located with the (immutable:false) release tarball, exactly the swappable-sibling case the comment says it avoids.
FIX: Bake the real digest into the released install.sh at tag time — local EXPECTED_SHA256="caa2dad0c7e1beaba313d4ec80c6f3e56ab0b6f19c0940de83d348fac7932ed6" — re-cut v0.1.0's install.sh, add the "set EXPECTED_SHA256 to the tarball's sha" step to the release runbook (README:300), and mark the GitHub release immutable so assets cannot be swapped.

## [LOW] cmd/run.sh:12  (core-verbs-a/correctness)
`agent-secrets run --` (separator present but no command) surfaces the internal helper's usage string, exposing an internal function name and the wrong command name instead of run.sh's user-facing usage.
FIX: In cmd/run.sh:12, assert a command follows the separator before delegating: `[ "${1:-}" = "--" ] && [ "$#" -ge 2 ] || agsec_die "usage: agent-secrets run -- <cmd> [args...]"`. This emits the user-facing message for both the missing-`--` and empty-command cases.</parameter>
</invoke>


## [LOW] lib/manifest.sh:161  (core-verbs-b/residue-after-uninstall)
When ~/.claude/settings.json did not exist before install, setup creates it as `{}` and records an edit whose backup is `{}`; rollback restores the backup rather than removing the tool-created file, leaving an orphaned empty settings.json.
FIX: In setup.sh `_wire_tools`, capture pre-existence before line 77: `local pre=1; [ -f "$sj" ] || pre=0`. When `pre=0` (tool created the file), do NOT record an edit-restore; instead record it for removal — e.g. add `created:true` to the edit record and have `_manifest_rb_edit` (manifest.sh:161) `rm -f "$p"` (removing the empty `.claude` dir too if now empty) when `created==true`, else keep the current restore path. Simplest equivalent: on the tool-created branch call `manifest_record_file "$sj"` so `_manifest_rb_file` deletes it on rollback, guarding the rm to only fire if the file still contains just the tool's apiKeyHelper edit (leave user-modified files as-is).

## [LOW] install.sh:74  (install-wrappers/security-trust-model)
The installer executes the Homebrew bootstrap from a moving `HEAD` ref with no verification, before its own SHA-verified artifact — directly contradicting the tool's stated "a moving ref is an unreviewed-code RCE" principle and the audit claim "never runs an unverified artifact."
FIX: Add one bullet to SECURITY.md's "honest ceiling" section stating the Homebrew bootstrap (install.sh:74) and its bottles are trusted-upstream and are NOT SHA-pinned/verified by this installer — only the tool's own release tarball is — and scope the install.sh:78 parenthetical to "never execute an unverified artifact of our own." Optionally note the same in _render_plan's "install Homebrew" line. No code/behavior change needed.

## [LOW] install.sh:56  (install-wrappers/dry-run-fidelity)
`--dry-run` prints a hand-maintained `_render_plan` summary and early-returns (line 56-61) rather than tracing the actual steps, and the summary has already drifted from what executes.
FIX: Complete _render_plan and kill the dead toggle: (a) add a bullet for the agent-secrets dispatcher symlink into $BIN_DIR (or fold it into the wrappers bullet as a 4th name), and a bullet for the conditional ~/.claude/settings.json → $STATE_DIR/settings.json.pre-install.bak backup; (b) drop the [d] branch's vestigial DRY_RUN=1/DRY_RUN=0 wrapping around _render_plan and either remove the option or relabel it (it re-shows the identical plan). Cleanest end-to-end fix: let --dry-run and [d] fall through the real code path so every _run/_step line (including 112 and 137) is the authoritative trace, keeping _render_plan only as the consent summary.

## [LOW] lib/manifest.sh:67  (install-wrappers/rollback-residue)
When the installer creates the shell rc file (e.g. ~/.zshenv) because it was absent, uninstall's pathblock strip writes back an (empty) file instead of deleting it, leaving an orphaned rc file — contradicting the "zero tool residue" claim.
FIX: In _manifest_rb_pathblock (manifest.sh:184-185), after computing `stripped`, if it contains no non-blank content, `rm -f "$file"` instead of writing back an empty file. To avoid deleting a user's own pre-existing empty rc file, gate the rm on an install-time flag: in manifest_pathblock_install (manifest.sh:66-68) capture whether the file pre-existed (`local created=0; [ -f "$file" ] || { created=1; : >"$file"; }`), pass it through manifest_record_pathblock into the pathblock record (`created:true|false`), and on rollback only `rm -f` when created==true AND the stripped remainder is empty.

## [LOW] README.md:87  (install-wrappers/doc-accuracy)
The "Exactly what it changes on your Mac" summary under-lists the real system changes — it omits the weekly launchd smoke job, the wrappers, and the ~/.claude/settings.json apiKeyHelper edit.
FIX: Either enumerate the full change-set in the callout — add the two extra wrappers (claude-agent, cursor-agent, apiKeyHelper), the weekly launchd smoke job, and the ~/.claude/settings.json apiKeyHelper edit — or soften "Exactly what it changes" to "The main things it change (full list in §3 and the on-screen install plan)." Optionally reflect that Keychain custody may fall back to the 0600 file on Sequoia rather than stating the key lives in the Keychain flatly.

## [LOW] lib/store.sh:79  (lib-foundation/doc-inaccuracy)
store_add_multiline's comment claims 'Like store_add the value never enters a shell variable', but store_add does read the value into a shell variable.
FIX: Reword the store_add_multiline comment to drop the false "Like store_add" equivalence, e.g.: "UNLIKE store_add (which stages the one-line value in a `value` shell variable before printing it, then unsets it), the multi-line path pipes STDIN straight through base64 into the 0600 plaintext temp, so the value never enters a shell variable or argv."

## [LOW] docs/FAQ.md:93  (docs-readme-faq/doc-false-claim)
'receive ... never touches a temp plaintext file' contradicts the implementation, which stages the decrypted value in a 0600 mktemp and decrypts the whole store into another temp.
FIX: Reword FAQ:92-93 to the accurate guarantee, e.g.: "`receive` decrypts the value only into a private 0600 temp that is shredded the instant it lands in your encrypted store — no durable plaintext file you have to remember to delete, and nothing in your scrollback." Drop the absolute "never touches a temp plaintext file."

## [LOW] SECURITY.md:76  (docs-agents-security-llms/misleading-framing)
SECURITY.md frames file custody as what happens 'If a macOS upgrade breaks the Keychain path' (a hypothetical future event), but per the tool's own code the automated Keychain populate already does not work on current macOS, so a default/unattended install runs on file custody now.
FIX: Reword SECURITY.md:73-75 to distinguish the two custody paths rather than presenting file custody as a future-only degradation: state that an attended `setup` populates the login Keychain via the interactive paste prompt (primary, prompt-free thereafter), while an automated/AGENT_SECRETS_UNATTENDED install (or a declined Keychain prompt) stores the key in the 0600 FileVault-backed file by default — a normal, doctor-tracked steady state that shows as "degraded (file custody)" and is fully supported, not an outage. Keep the macOS-upgrade sentence only as one additional trigger for file custody, not the sole one.

## [LOW] bin/agent-secrets:74  (help-selfdescribe/undocumented-alias)
The dispatcher exposes an `ls` alias for `list` and a `version` subcommand, but neither appears in help.sh or the `help --json` manifest, contradicting the 'fully self-describing, no human needed' claim.
FIX: Make the authoritative manifest cover every live route. Simplest: (a) add the `ls` alias to the list command's help spec (e.g. an `aliases`/synopsis note) and map `help ls`→list in the render path so `help ls` stops erroring; and (b) drop the redundant `version)` subcommand case at bin/agent-secrets:83 in favor of the already-documented `-V/--version` flag (or, if kept, add a `version` entry to help.sh). Either close the doc gap or remove the undocumented routes so `help --json` is genuinely the complete surface.

## [LOW] install.sh:97  (shell-robustness-all/release-integrity)
install.sh's `tar --strip-components=1` hard-assumes a single top-level prefix dir in the release tarball, but no committed release recipe pins `git archive --prefix=...` and CI never exercises install-from-tarball, so re-cutting with a bare `git archive HEAD` would flatten the layout and dangle every wrapper symlink.
FIX: Two cheap defenses: (1) Pin the prefix in the release recipe — change README.md:300 (and ideally add a committed release script) to `git archive --prefix=agent-secrets-${TAG}/ ${TAG} -o agent-secrets-${TAG}.tar.gz`; (2) add a post-untar sanity guard in install.sh between lines 97 and 101, e.g. `[ -f "$INSTALL_DIR/lib/common.sh" ] || _die "unexpected tarball layout (need a single top-level prefix dir)"`, so a mis-cut tarball fails loudly instead of aborting cryptically on the `set -e` source. Optionally add a CI job that builds the tarball and installs it into a synthetic HOME.

## [LOW] docs/FAQ.md:92  (security-invariant-e2e/misleading-doc)
FAQ claims receive 'never touches a temp plaintext file', but receive writes the decrypted plaintext to a 0600 temp on disk before storing it.
FIX: Reword FAQ.md:92-93 to stop claiming no temp exists: e.g. "`receive` decrypts through a private 0600 temp in the encrypted-config dir that is shredded on exit (FileVault-backed erasure) and pipes straight into the sops store — the value never reaches scrollback, stdout, or a persistent/user-visible file." This keeps the true contrast with `age -d >file` (which leaves a persistent plaintext file) without the false "never touches a temp plaintext file" absolute. Consider the same tightening at FAQ.md:78.

## [LOW] lib/help.sh:30  (security-invariant-e2e/exit-code-inconsistency)
Top-level help documents exit 2 for usage errors, but the v0.1 verbs (add/list/run/doctor) return exit 1 on their own usage errors while only the dispatcher and newer share/receive/pubkey use exit 2.
FIX: Pass explicit exit code 2 to agsec_die on the usage/invalid-flag/invalid-name/missing-separator paths: add.sh:15 (missing NAME) and :17 (invalid NAME), list.sh:16 (unknown arg/flag), run.sh:12 (missing `--`), doctor.sh:22 (unknown flag) — e.g. agsec_die "..." 2. This aligns the v0.1 verbs with share/receive/pubkey and the documented top-level convention. (Alternatively/additionally, add the exit-2 usage code to the add/list/run/doctor per-verb exit specs in help.sh so the manifest is coherent.)

## [LOW] cmd/setup.sh:92  (security-invariant-e2e/doc-gap)
The install tarball ships no docs (.gitattributes export-ignores them), yet setup and help point installed users to local README/AGENTS/SECURITY files that do not exist under the install dir.
FIX: Prefer pointing the installed references at canonical pinned GitHub URLs (aligns with the deliberately runtime-only tarball): change cmd/setup.sh:92 "Docs: README.md." to "Docs: https://github.com/renchris/agent-secrets" and lib/help.sh's seealso rows (lines 31-32) to full URLs (e.g. https://github.com/renchris/agent-secrets/blob/v0.1.0/AGENTS.md and .../SECURITY.md). Alternatively drop the export-ignore on those three docs + llms.txt so they ship under the install dir. The URL approach is the more minimal, tarball-hygiene-preserving fix.

## [LOW] .gitattributes:3  (release-integrity/release-hygiene-doc-inconsistency)
The .gitattributes header claims "Dev tooling, docs, CI ... are excluded from the tarball," but the published release tarball ships scripts/telemetry-gate.sh (a CI-only gate) and scripts/record-demo.sh (a maintainer-only demo-GIF tool), so the tarball is not "runtime only" and the audit invariant "nothing dev leaks in" is false.
FIX: Make the archive and its self-description agree. Minimal: add `scripts/record-demo.sh export-ignore` (it is broken dead-weight — its assets/demo.tape and tests/mocks refs are stripped) and `scripts/telemetry-gate.sh export-ignore`, keeping scripts/keychain-smoke.sh as a legitimate operational gate; then reword the .gitattributes:2-3 comment so it no longer asserts "CI ... excluded" / "RUNTIME only" while wholesale-shipping scripts/*.sh. (Alternatively, if telemetry-gate.sh is intentionally shipped as a user-runnable no-exfil audit — it says "safe to run locally" — keep it but drop the "CI ... excluded / RUNTIME only" phrasing and enumerate the shipped audit/operational scripts explicitly.)

## [LOW] install.sh:74  (release-integrity/install-flow-inconsistency)
install.sh bootstraps Homebrew by piping a MOVING-ref script (Homebrew/install/HEAD/install.sh) straight into /bin/bash, directly contradicting the file's own stated anti-moving-ref security principle (line 6 "Install from a PINNED release tag (never main)"; line 13 "a moving ref is an unreviewed-code RCE").
FIX: Do NOT pin Homebrew's installer to a commit — Homebrew designs its installer to run from HEAD and a pinned SHA would break/rot. Instead reconcile principle and practice with one line: either (a) add a comment at line 74 marking it an explicit accepted-standard exception ("# Homebrew's canonical HEAD bootstrap is the ecosystem-standard trust anchor; pinning its installer is unsupported and fragile — this is the one intentional moving-ref exception to the rule above"), or (b) narrow line 6's wording from "verify any artifact" to "verify the tool's release artifact" so the stated principle no longer over-claims coverage it doesn't provide for third-party dependency bootstraps.

