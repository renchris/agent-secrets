#!/usr/bin/env bash
# cmd/setup.sh — onboarding wizard (screens 0–7, resume-state, restore branch).
# Sources: common.sh, ui.sh, store.sh, keychain.sh, manifest.sh.
set -euo pipefail
. "${AGENT_SECRETS_LIB:?run via bin/agent-secrets}/common.sh"
agsec_die "setup: not implemented yet"
